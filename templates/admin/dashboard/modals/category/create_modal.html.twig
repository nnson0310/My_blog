<div class="modal" id="create-modal">
	<div class="modal-dialog">
		<div
			class="modal-content">
			<!-- Modal Header -->
			<div class="modal-header">
				<h4 class="modal-title">Create New Category</h4>
			</div>
			<!-- Modal body -->
			<div class="modal-body">
				<form action="{{ path('admin_category_create') }}" id="edit-form" method="POST">
					<input type="hidden" name="_token" value="{{ csrf_token('create_category') }}">
					<div class="form-group">
						<label for="cat_name">Name</label>
						<input class="form-control" type="text" name="cat_name" {% if app.session.get('cat_name') %} value="{{ app.session.get('cat_name') }}" {% endif %}>
						{% for msg in create_error_name %}
							<small class="form-text text-muted text-danger">
								{{ msg }}
							</small>
						{% endfor %}
					</div>
					<div class="form-group">
						<label for="cat_desc">Description</label>
						<textarea class="form-control" name="cat_desc">{% if app.session.get('cat_desc') %} {{ app.session.get('cat_desc') }} {% endif %}</textarea>
						{% for msg in create_error_desc %}
							<small class="form-text text-muted text-danger">
								{{ msg }}
							</small>
						{% endfor %}
					</div>
					<diV class="text-center">
						<button type="button" class="btn btn-success float-left" data-dismiss="modal">Close</button>
						<button type="submit" class="btn btn-primary" id="btnUpdateSubmit">Create</button>
					</div>
				</form>
			</div>
		</div>
	</div>
</div>

